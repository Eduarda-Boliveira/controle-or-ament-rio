meta {
  name: Get Transaction by ID
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/transactions/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  baseUrl: http://localhost:3000/api
}
